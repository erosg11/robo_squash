function [ v_ball ] = add_impulse( v_ball, v_raquete, D, f_raquete, regeneration, m_ball )
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here

    K = (4/3) * (1 - poison ^2)/young;
    
    h = ((5 * m_ball * norm(v_ball - v_raquete) ^ 2)/ ...
    (4 * (D/2) ^0.5 * K))^2/5;

    tempo = h / norm(v_ball - v_raquete);
    
    

end

